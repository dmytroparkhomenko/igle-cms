{
  "name": "@igle/html-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@igle/shared": "workspace:*",
    "cheerio": "^1.1.2",
    "magic-string": "^0.30.18",
    "parse5": "^7.3.0"
  }
}
